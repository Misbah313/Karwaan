class: Card
table: card
fields:
  title: String
  description: String?
  createdBy: int, relation(parent=user)
  list: int, relation(parent=listboard)
  createdAt: DateTime
  position: int?
  isCompleted: bool